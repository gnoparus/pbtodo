name: 🚀 Deploy to Production

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      reason:
        description: "Reason for manual production deployment"
        required: false
        default: "Manual production deployment"

env:
  NODE_VERSION: "20"

jobs:
  # Job 1: Test and Build
  test-and-build:
    name: 🧪 Test & Build
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: 📦 Install Root Dependencies
        run: npm ci

      - name: 📦 Install Workers Dependencies
        run: cd workers && npm install

      - name: 🔧 TypeScript Check (Workers)
        run: cd workers && npm run type-check

      - name: 🔧 TypeScript Check (Frontend)
        run: npm run type-check

      - name: ✨ Lint Check
        run: npm run lint
        continue-on-error: true

      - name: 🧪 Run Unit Tests
        run: npm run test:unit
        continue-on-error: true

      - name: 🔨 Build Frontend
        run: npm run build
        env:
          VITE_API_URL: ${{ secrets.VITE_API_URL || 'https://pbtodo-api.workers.dev/api' }}
          VITE_ENVIRONMENT: production
          VITE_DEV_MODE: false
          VITE_HTTPS_ENABLED: true
          VITE_ENABLE_SECURITY_HEADERS: true
          VITE_ENABLE_CSP: true
          VITE_ENABLE_HSTS: true

      - name: 📦 Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: frontend/dist/
          retention-days: 7

  # Job 2: Deploy Workers API
  deploy-workers:
    name: 🚀 Deploy Workers API
    runs-on: ubuntu-latest
    needs: test-and-build
    timeout-minutes: 10
    permissions:
      contents: read
    environment:
      name: production
      url: https://pbtodo-api.workers.dev

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: 📦 Install Workers Dependencies
        run: cd workers && npm install

      - name: 🚀 Deploy to Cloudflare Workers
        run: cd workers && npx wrangler deploy --env production
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: ✅ Workers Deployment Complete
        run: |
          echo "🎉 Workers API deployed successfully!"
          echo "📍 URL: https://pbtodo-api.workers.dev"

  # Job 3: Deploy Frontend to Pages
  deploy-pages:
    name: 🚀 Deploy Frontend to Pages
    runs-on: ubuntu-latest
    needs: [test-and-build, deploy-workers]
    timeout-minutes: 10
    permissions:
      contents: read
      deployments: write
    environment:
      name: production
      url: https://pbtodo-frontend.pages.dev

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: 📦 Download Build Artifacts
        uses: actions/download-artifact@v4
        with:
          name: frontend-build
          path: frontend/dist/

      - name: 📦 Install Wrangler
        run: npm install -g wrangler

      - name: 🚀 Deploy to Cloudflare Pages
        run: |
          cd workers
          npx wrangler pages deploy ../frontend/dist \
            --project-name=pbtodo-frontend \
            --branch=main \
            --commit-dirty=true
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}

      - name: ✅ Pages Deployment Complete
        run: |
          echo "🎉 Frontend deployed successfully!"
          echo "📍 URL: https://pbtodo-frontend.pages.dev"

  # Job 4: Health Check
  health-check:
    name: 🏥 Health Check
    runs-on: ubuntu-latest
    needs: [deploy-workers, deploy-pages]
    timeout-minutes: 5

    steps:
      - name: ⏳ Wait for Deployment Propagation
        run: sleep 30

      - name: 🏥 Check Workers API Health
        run: |
          echo "🏥 Checking Workers API health..."

          max_retries=5
          retry_count=0

          while [ $retry_count -lt $max_retries ]; do
            if curl -f -s https://pbtodo-api.workers.dev/api/health > /dev/null; then
              echo "✅ Workers API is healthy"
              exit 0
            else
              echo "⏳ Retry $((retry_count + 1))/$max_retries..."
              retry_count=$((retry_count + 1))
              sleep 10
            fi
          done

          echo "❌ Workers API health check failed after $max_retries retries"
          exit 1

      - name: 🏥 Check Pages Health
        run: |
          echo "🏥 Checking Pages health..."

          max_retries=5
          retry_count=0

          while [ $retry_count -lt $max_retries ]; do
            response=$(curl -s -o /dev/null -w "%{http_code}" https://pbtodo-frontend.pages.dev)
            if [ "$response" = "200" ]; then
              echo "✅ Pages is healthy"
              exit 0
            else
              echo "⏳ Retry $((retry_count + 1))/$max_retries... (HTTP $response)"
              retry_count=$((retry_count + 1))
              sleep 10
            fi
          done

          echo "❌ Pages health check failed after $max_retries retries"
          exit 1

  # Job 5: Deployment Summary
  deployment-summary:
    name: 📊 Deployment Summary
    runs-on: ubuntu-latest
    needs: [deploy-workers, deploy-pages, health-check]
    if: always()

    steps:
      - name: 📊 Generate Deployment Summary
        run: |
          echo "# 🚀 Production Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📅 Deployment Info" >> $GITHUB_STEP_SUMMARY
          echo "- **Date**: $(date -u +"%Y-%m-%d %H:%M:%S UTC")" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit**: \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Branch**: \`${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Triggered by**: @${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 🌐 Deployment URLs" >> $GITHUB_STEP_SUMMARY
          echo "- **Workers API**: https://pbtodo-api.workers.dev" >> $GITHUB_STEP_SUMMARY
          echo "- **Frontend**: https://pbtodo-frontend.pages.dev" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## ✅ Status" >> $GITHUB_STEP_SUMMARY
          echo "| Component | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-----------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Workers API | ${{ needs.deploy-workers.result == 'success' && '✅ Deployed' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Frontend | ${{ needs.deploy-pages.result == 'success' && '✅ Deployed' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Health Check | ${{ needs.health-check.result == 'success' && '✅ Passed' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "${{ needs.health-check.result }}" = "success" ]; then
            echo "## 🎉 Deployment Successful!" >> $GITHUB_STEP_SUMMARY
            echo "All services are deployed and healthy." >> $GITHUB_STEP_SUMMARY
          else
            echo "## ⚠️ Deployment Issues" >> $GITHUB_STEP_SUMMARY
            echo "Some services may not be fully operational. Check the logs for details." >> $GITHUB_STEP_SUMMARY
          fi

      - name: 🎉 Success Notification
        if: needs.health-check.result == 'success'
        run: |
          echo "🎉 Production deployment completed successfully!"
          echo "📍 Workers API: https://pbtodo-api.workers.dev"
          echo "📍 Frontend: https://pbtodo-frontend.pages.dev"

      - name: ❌ Failure Notification
        if: needs.health-check.result != 'success'
        run: |
          echo "❌ Production deployment failed or has issues!"
          echo "Check the logs for details"
          exit 1
