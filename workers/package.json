{
  "name": "pbtodo-workers",
  "version": "1.0.0",
  "description": "Cloudflare Workers API for PBTodo",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "tail": "wrangler tail",
    "test": "vitest",
    "test:watch": "vitest watch",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.10.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250104.0",
    "@types/node": "^22.10.5",
    "typescript": "^5.7.2",
    "vitest": "^1.6.0",
    "wrangler": "^4.45.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
